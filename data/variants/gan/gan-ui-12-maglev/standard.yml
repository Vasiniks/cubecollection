# variant: gan-ui-12-maglev--standard
# Pass 4. "GAN 12 ui Maglev" — a smart cube built on the mechanical GAN12 base with a MagLev
# tensioning system, per TheCubicle's own retail description ("built like a GAN 12... with a
# MagLev tensioning system").
id: gan-ui-12-maglev--standard
entity: variant
model_id: gan-ui-12-maglev
name: "GAN 12 ui Maglev"
scope_class: core
edition:
  types: [standard, smart]
smart:
  is_smart: true
config:
  maglev: maglev
status: stub
attestations:
  /edition/types:
    confidence: probable
    sources: [thecubicle-gan12-ui-maglev-10th-anniversary-2024]
    note: >-
      ASSESSMENT UPDATED 2026-09-11 during depth research. This was a lone baseline; the
      gan12-ui slug sweep found a named 10th Anniversary Edition in transparent plastic, now
      recorded as a sibling. This record is the standard-plastic configuration.
      TWO FURTHER PATHS ON THIS LINE WERE CHECKED AND REFUSED as bundles rather than
      configurations: gan12-ui-maglev-with-powerpod and gan12-ui-maglev-3x3-powerpod-2026-
      edition pair the same cube with GAN's charging dock. What differs is box contents, and
      this archive excludes a bundle from the variant layer.
  /config/maglev:
    confidence: probable
    sources: [gancube-collections-ui-series]
    note: "Edition name itself ('ui Maglev'); a retailer listing independently describes it as built like GAN12 'with a MagLev tensioning system' (not cited as a canonical source this pass — see research/notes/variants/gan-pass4.md)."
