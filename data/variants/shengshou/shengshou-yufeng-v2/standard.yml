# variant: shengshou-yufeng-v2--standard
# Pass 4 BASELINE, created by the P4-3 normalisation (research/qc/p4-3-variant-semantics.md).
# This model was ASSESSED for configuration axes by pass4-agent-d-dayan-shengshou and exactly one
# configuration was found. This record is that configuration - the model's regular retail
# form - and it exists because pricing, colorway, media, packaging, rarity, specimen
# ownership and the public bundle all attach to the variant layer, never to the model.
# A model with no variant cannot be exhibited at all.
#
# It does NOT assert that a product marketed as "Standard" was sold. It asserts that the
# model was researched and no differentiated configuration was established.
id: shengshou-yufeng-v2--standard
entity: variant
model_id: shengshou-yufeng-v2
name: "ShengShou YuFeng V2 3x3 M"
scope_class: core
edition:
  types: [standard]
status: stub
attestations:
  /edition/types:
    confidence: probable
    sources: [speedcubeshop-shengshou-yufeng-rename-2026]
    note: >-
      DIFFERENTIATION SEARCH RECORDED 2026-09-11. SpeedCubeShop lists FOUR YuFeng 3x3 product
      paths against this archive's two models, which reads as two missing configurations. It
      is not. The two V2 paths carry the IDENTICAL SKU and word-for-word identical
      descriptions; the listing was renamed. Rejected as a configuration and preserved as a
      rejection, because evidence for a rejected finding has nowhere else to live and the
      next pass would otherwise re-derive the same false lead from the same sweep source.
      This record therefore remains a one-configuration baseline on a SEARCHED result.