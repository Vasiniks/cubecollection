# variant: qiyi-m-pro-standard--maglev
# Pass 4. The "MagLev" configuration, per TheCubicle's own separate product page
# (thecubicle-qiyi-m-pro-3x3-maglev-product), already cited by the model record as a variant of
# this model (not a separate model), consistent with the wiki's own "multiple versions" framing.
id: qiyi-m-pro-standard--maglev
entity: variant
model_id: qiyi-m-pro-standard
name: "QiYi M Pro 3x3 (MagLev)"
scope_class: core
edition:
  types: [standard]
  designation: "MagLev"
config:
  maglev: maglev
  weight_g: 80.0
status: stub
attestations:
  /edition/types:
    confidence: probable
    sources: [thecubicle-qiyi-m-pro-3x3-maglev-product]
  /edition/designation:
    confidence: probable
    sources: [thecubicle-qiyi-m-pro-3x3-maglev-product]
  /config/maglev:
    confidence: probable
    sources: [thecubicle-qiyi-m-pro-3x3-maglev-product]
    note: "'a maglev tension system' is TheCubicle's own direct description."
  /config/weight_g:
    confidence: probable
    sources: [thecubicle-qiyi-m-pro-3x3-maglev-product]
    note: >-
      SPEC-ADVERSARIAL LANE J REPAIR. This variant carried NO weight_g override and was
      silently inheriting the base model's 76.0g (qiyi-m-pro-standard's own non-magnetic-page
      figure) — wrong for this configuration. This source's own spec table states "Item Weight:
      80.0g" (Dimensions 55.0mm, matching this line's size; "Gross Weight: 143g" on the same
      table is the packaged figure, refused under the rule-45 class). The model record's own
      /specs/weight_g note had already identified this exact figure ("the MagLev figure belongs
      to that configuration and is left for a variant that records it") during audit sweep #14
      on 2026-09-12, but no variant was ever updated to record it — the evidence was preserved
      and even cited in prose, but never written into config. Recorded here to close that loop.
