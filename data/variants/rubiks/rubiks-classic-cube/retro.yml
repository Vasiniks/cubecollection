# variant: rubiks-classic-cube--retro
# Pass 4 batch 2, Agent E. "Rubik's Retro" — a 50th-anniversary commemorative treatment of the
# classic mechanism ("classic boxy edges, slower turning, stickers, gold side, and special
# anniversary logo, presented in a retro plastic display case"), per the family record's own
# pass-2 boundary call. Admitted as a variant, not a separate model, per DATA_MODEL §4.2 — the
# "classic boxy edges, slower turning" language describes a deliberately old-style mechanism
# feel, not a stated distinct mould/tooling claim.
#
# `edition/commemorates` is left unset: this variant commemorates Rubik's own 50th anniversary,
# but no `event` record for that anniversary exists in data/events/ and creating one is outside
# this pass's write lane (data/variants/**, data/sources/*.yml only). Flagged in the pass
# report as a gap for whoever next works data/events/.
id: rubiks-classic-cube--retro
entity: variant
model_id: rubiks-classic-cube
name: "Rubik's Retro"
scope_class: core
edition:
  types: [commemorative, anniversary]
  designation: "Retro"
colorway:
  designation: "Retro"
  scheme: custom
  application: stickered
status: stub
attestations:
  /edition/types:
    confidence: confirmed
    sources: [rubiks-com-classic-special-editions-2024]
    note: "Rubik's own page: 'Launched to celebrate Rubik's 50th anniversary, this special
      edition Rubik's Cube looks and feels like it came right out of a time capsule!'"
  /edition/designation:
    confidence: confirmed
    sources: [rubiks-com-classic-special-editions-2024]
    note: "Rubik's own product page name: 'Rubik's Retro.'"
  /edition/commemorates:
    confidence: unknown
    note: "No `event` record for Rubik's 50th anniversary exists in this archive; the fact is
      recorded in `edition/types` and in this note, but the relational link to an `event`
      entity is left unresolved pending an events-pass record (outside this session's write
      lane)."
  /colorway/designation:
    confidence: confirmed
    sources: [rubiks-com-classic-special-editions-2024]
  /colorway/application:
    confidence: probable
    sources: [rubiks-com-classic-special-editions-2024]
    note: "Rubik's own page names 'stickers' explicitly among the retro-styled features,
      contrasted against the base model's stickerless tiles."
