# model: gan-ui-12-freeplay
# Pass 3 (model enumeration). "GAN12 UI FreePlay Smart Speed Cube", sold at its own dedicated
# URL — the smart, flagship-based sibling of the mechanical GAN12 (gan-flagship-12), distinct
# from GAN12 ui Maglev and GAN12 ui SP, which are separate catalog listings without a shared
# "Versions" selector. See research/notes/models/gan-pass3.md.
id: gan-ui-12-freeplay
entity: model
family_id: gan-ui-series
manufacturer_id: gan
name: "GAN12 ui FreePlay"
scope_class: core
generation:
  label: "12 ui FreePlay"
  ordinal: 12
  basis: community_convention
announced:
  value: "2024-11"
  precision: month
  qualifier: before
description: |-
  GAN's own product page for "GAN12 UI FreePlay Smart Speed Cube", sold at a dedicated URL
  distinct from "GAN12 ui Maglev" and "GAN12 ui SP". This model's own page still resolved as
  late as 2026-02-08 (a later capture of the same URL, not independently preserved as its own
  source this pass), after "GAN12 ui SP" had already launched (2025-07-31) — evidence the two
  coexisted rather than SP simply replacing FreePlay. See gan-ui-12-sp's own description.
status: drafted
attestations:
  /announced:
    confidence: uncertain
    sources: [gancube-gan12-ui-freeplay-product]
    note: "This capture (2024-11-19) falls inside gancube.com's own site-wide November 2024 archival event, so it is treated as a weak floor only — the product could be considerably older."
  /generation/basis:
    confidence: uncertain
    note: "'FreePlay' is a tier word, not a stated sequential generation numeral; the '12' is GAN's own borrowed reference to the mechanical GAN12 base, not an independently-declared ui-series generation count of its own."
